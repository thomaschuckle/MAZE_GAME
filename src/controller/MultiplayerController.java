package controller;

import java.util.ArrayList;

import model.Card;
import model.Chat;
import model.GameState;
import model.Maze;
import model.Player;
import model.ScoreManager;
import view.CardPanel;
import view.ChatPanel;
import view.ControlPanel;
import view.InfoPanel;
import view.MazePanel;
import view.MenuPanel;

public class MultiplayerController {
	
    public MultiplayerController(ArrayList<Card> cardList, ArrayList<Player> playerList, ScoreManager scoreManagerModel,
            Chat chatModel, Maze mazeModel, GameState gameStateModel, CardPanel cardPanel, ChatPanel chatPanel,
            ControlPanel controlPanel, InfoPanel userInfoPanel, MazePanel mazePanel, MenuPanel menuPanel) {
        // TO_DO
    }

    public void setupHostGame() {
    	
    }
    
    public void setupJoinGame() {
    	
    }
}
